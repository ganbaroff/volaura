---
type: audit
title: Implement Energy Adaptation (Law 2) UI Mode Logic
perspectives: all
---
Review the entire VOLAURA web application codebase (specifically the client-side rendering logic in `frontend/src/components/Layout.jsx` and related context providers) to ensure the three UI modes (Full, Mid, Low) are correctly enforced. The implementation must be governed by the `EnergyLevel` state variable (sourced from the user profile or session context). The audit must confirm that *all* components, including the public profile, assessment widget, and organization search, conditionally rend
