# Archived Protocols

Moved here 2026-04-12 during theater cleanup.

These files were 1534 lines of process documentation with zero automated enforcement.
The hook that was supposed to enforce TASK-PROTOCOL (protocol-enforce.sh) created a
catch-22 that blocked all production edits. It was deleted in the same cleanup.

If a protocol can't be enforced by a hook, it doesn't exist as a rule — it exists as
a wish. These files remain in git history for reference.

Kept in active docs: QUALITY-STANDARDS.md (146 lines, has useful DoD checklist).
