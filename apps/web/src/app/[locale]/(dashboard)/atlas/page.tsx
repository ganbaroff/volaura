"use client";

import { ProductPlaceholder } from "@/components/ui/product-placeholder";

export default function AtlasPage() {
  return (
    <ProductPlaceholder
      name="ATLAS"
      icon="⚡"
      tagline="The orchestrator. One gateway to manage all ecosystem products."
      accentVar="var(--color-product-atlas)"
    />
  );
}
