{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "rm -rf apps/web/.next/cache && pnpm --filter @volaura/web build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "regions": ["fra1"],
  "git": {
    "deploymentEnabled": true
  },
  "ignoreCommand": "bash -c 'if [ -z \"$VERCEL_GIT_PREVIOUS_SHA\" ]; then exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" HEAD -- apps/web/ packages/ pnpm-lock.yaml pnpm-workspace.yaml .env.md vercel.json .vercelignore .nvmrc'"
}
