# Atlas Proactive — Init seed note

**Date:** 2026-04-12T00:00:00Z
**Wake number:** 0000 (pre-cron seed)
**Topic ID:** init
**Priority:** seed
**Provider used:** main Atlas instance, hand-written
**Follow-up action:** verify cron fires, replace this note with real first wake output

---

This is the seed note for the Atlas proactive inbox. It exists to prove the directory is writable, the wake-ritual inbox read step has something to point at on its first execution, and the git commit path is clean.

**Infrastructure status at creation:**
- `memory/atlas/proactive_loop.md` — spec committed
- `memory/atlas/proactive_queue.json` — 7 seed topics queued, 4 high priority
- `packages/swarm/atlas_proactive.py` — Phase 1 worker (heartbeat only, no real LLM call)
- `.github/workflows/atlas-proactive.yml` — cron every 15 minutes, scope guard for write safety
- This inbox file — seed note

**What main Atlas should do on its next wake:**
1. Read this note.
2. Confirm cron workflow `Atlas Proactive Loop` appears in `.github/workflows/` on GitHub.
3. Check workflow run history at `https://github.com/ganbaroff/volaura/actions/workflows/atlas-proactive.yml` after first 15-minute tick.
4. If the first real wake produced a heartbeat note (Phase 1 output), mark this seed as `Consumed` in the footer and move to Phase 2 wiring.
5. Phase 2: extend `packages/swarm/atlas_proactive.py` to import `apps.api.app.services.model_router.select_provider`, make a real NVIDIA Nemotron call, and replace the `_phase1_heartbeat_body` function with a real research generator.

**What Phase 2 requires from the environment:**
- The GitHub Actions runner needs to install `app/services/model_router.py` dependencies (pydantic, loguru, and the LLM provider client libs). Today the workflow only has stdlib. Phase 2 step: `pip install pydantic loguru httpx` at minimum.
- The environment variables (NVIDIA_API_KEY, GEMINI_API_KEY, GROQ_API_KEY) must be set as repo secrets. They already are — confirmed in the existing `session-end.yml` workflow.

**Known limits of the Phase 1 heartbeat:**
- It does not do real research. It writes a deterministic note acknowledging the topic.
- It does not call any LLM.
- It does not read the web.
- It does not verify external facts.
- Its value is only the proof that the pipeline fires and commits successfully.

**The first real research topic, already in the queue and due:** `latest-ai-agent-memory-tools-2026`. CEO specifically asked Atlas to find the top AI-agent memory and orchestration repos released in 2025-2026 with >175k-200k stars equivalent signal. That is the first topic the Phase 2 loop will process.

---

**Consumed by main Atlas:** pending (will be marked consumed on next wake)
**Result:** pending (will be marked after next wake — either "infrastructure live, Phase 1 heartbeat observed" or "cron not firing, investigate")
