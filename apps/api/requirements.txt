# Core
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.34.0
supabase>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
python-jose[cryptography]>=3.3.0

# Rate limiting
slowapi>=0.1.9

# HTTP
httpx>=0.28.0

# LLM
google-genai>=1.0.0
openai>=1.60.0

# Assessment (IRT/CAT engine is pure-Python, no external dep needed)
numpy>=1.24.0

# Notifications
python-telegram-bot>=21.0

# Logging
loguru>=0.7.0

# File uploads
python-multipart>=0.0.20

# BrandedBy — fal.ai video generation (MuseTalk + PlayAI TTS)
fal-client>=0.5.0

# Observability — LLM tracing (optional, graceful degradation if keys not set)
langfuse>=2.0.0

# Swarm orchestration — structural enforcement of delegation (ADR-009)
crewai>=0.100.0
