# HANDOFF — 2026-05-23 20:44 AST

Next instance: read THIS file. then ONE next action. then ask CEO. nothing else.

## Voice rule
caveman. short. time stamp every message. think then act. step-by-step. ONE thing only. no walls.

## ONE outcome
MindShift internal-test ship. same-day. NOT production.

## ONE next action (right now)
CEO sets 3 env vars in his PowerShell:
```powershell
$env:MINDSHIFT_KEYSTORE_PASSWORD = "..."
$env:MINDSHIFT_KEY_ALIAS = "mindshift"
$env:MINDSHIFT_KEY_PASSWORD = "..."
```
keystore already exists at `C:/Projects/mindshift/android/release.keystore`. alias `mindshift` set. CEO knows keystore password.

CEO reply: `env done`.

## Then what
do NOT plan ahead. wait for `env done`. then ask CEO. give ONE next thing.
all later steps + state in `codex-loop.md` iteration 11. do not preload them.

## Hard ground (verified facts only)
- mindshift main = `93ada513` (PR #21 merged this session).
- PR #22 head = `56d5b71`. awaiting Codex re-review. mergeable CLEAN.
- 12 errors logged this session. classes 14/22/26/28/34. see `codex-loop.md` iteration 9.
- 460 AZN + $7.25 USD + 1 narrative credit credited-pending. DEBT-004 in ledger NOT committed.
- wake protocol step 10.3 + 11 skipped this session.

## Do NOT
- write walls of text to CEO
- give multi-step plans
- skip time stamp
- claim done without tool call same turn
- touch VOLAURA `codex/swarm-queue-bridge` dirty files
- regenerate keystore (it exists)
- spawn 4 parallel agents unless single bounded outcome demands it
- ignore octogent/vellum/openmanus as "utopia" — they exist on disk

## Channel
`codex-loop.md` iteration N. Codex odd, Claude even. last claude write = 11. last codex = 10.

## Tool truth
time of this write: `2026-05-23 20:44 AST` per Bash date.
