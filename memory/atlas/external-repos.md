---
name: External repos for Atlas sessions
description: Three repos CEO wants Atlas to work in — LifeSimulator (Godot), ANUS (AI CLI agent, Node.js), OpenManus (Python agent framework). Each gets dedicated session.
type: project
---

## CEO directive 2026-04-18: Three repos, three sessions

CEO will launch Atlas in each directory separately. Tasks: анализ → доработка → подтверждение готовности.

### 1. LifeSimulator — `C:\Users\user\OneDrive\Desktop\BestGame\life-simulator-2026`

**What:** Godot 4 game. One of VOLAURA's 5 product faces (LifeSim = narrative rendering of user's life).
**Stack:** GDScript, 431 .gd files, 425 .tscn scenes.
**Git:** Last commit `11e7f56` — VOLAURA in-game auth flow (login screen + JWT session).
**Prior work:** VOLAURA cross-product bridge (Sprint E3), P0 red color fix, API URL wiring.
**VOLAURA integration:** Auth flow exists, API URL in project settings.
**Key docs in repo:** PROJECT_SUMMARY.md, LAUNCH_GUIDE.md, WALKTHROUGH.md, ANDROID_EXPORT_STEP_BY_STEP.md.

**P0 BUG FOUND (2026-04-18):** Main scene `main_menu_simple.tscn` CRASHES — `VolauraAPIClient` type not found in scope. The class IS defined in `scripts/managers/api_client.gd` with `class_name VolauraAPIClient`, and `Character` model exists with all required methods (`sync_crystals`, `apply_volaura_boosts`, `set_aura_level_from_score`). But Godot 4.6.1 fails to resolve the type at parse time. Likely cause: circular dependency or parse-order issue in Godot 4.6.1 vs 4.4/4.5 the project was built for. All other controllers (App, Character, EventQueue, Relationship, Career, Idle, Analytics, Monetization, SaveLoad, Time) initialize fine.

**Session goal:** Fix VolauraAPIClient parse error. Then: assess Android readiness, auth flow, cross-product bridge.

### 2. ANUS — `C:\Users\user\OneDrive\Documents\GitHub\ANUS`

**What:** ANUS (Autonomous Networked Utility System) CLI — Grok-powered AI terminal agent. Generated by Manus (AI). Self-improving agent concept.
**Stack:** Node.js monorepo (packages/cli, packages/core, packages/vscode-ide-companion). npm workspaces.
**Git:** Last commit `74fef7c` — release v0.1.0. Only 5 commits total (2 are merge/fix).
**License:** Apache-2.0. Published to npm as `@anus-dev/anus`.
**Origin:** Created by Manus AI, then forked/continued. Community: Telegram @anusonmars, X @anusonmars.

**Session goal:** Assess code quality, understand architecture, identify what's reusable for ZEUS swarm agents. Grok-powered CLI agent → can it become a VOLAURA swarm agent?

### 3. OpenManus — `C:\Users\user\OneDrive\Desktop\openmanus\OpenManus`

**What:** MIT-licensed Python agent framework from MetaGPT team. ZEUS skeleton.
**Stack:** Python 3.12 (requirements say), but CEO's venv is Python 3.14 → dependency build failures (pydantic_core, tiktoken, pillow).
**Status:** config.toml set to Ollama (qwen3:8b + gemma4 vision). Dependencies NOT fully installed yet.
**Architecture:** BaseAgent → ReActAgent → ToolCallAgent → Manus. Tools: bash, python, browser, file ops, search, MCP. Sandbox: Docker per agent.

**Session goal:** Get it running. Fix Python version issue (either downgrade venv to 3.12 or find compatible packages). Run first agent task.

### Hardware context (all sessions)
- GPU: RTX 5060 Laptop, 8GB VRAM, CUDA 13.2
- Ollama: gemma4, glm-ocr, qwen3:8b
- Whisper: base model on GPU confirmed working
- torch 2.11+cu128

### Vercel deploy status (2026-04-18 Session 118)
Webhook reconnected — Git pushes trigger builds. But ALL builds fail with `module_not_found`.
Build command changed from `cd apps/web && pnpm build` to `pnpm --filter apps/web build` (commit 82ec788).
.nvmrc added pinning Node 20 LTS.
.vercelignore added excluding models/ remotion/ api/.
Root cause unconfirmed — build passes locally, fails on Vercel. Likely pnpm workspace resolution difference.
Next step: check if latest push (82ec788) built successfully. If not, try setting Root Directory to apps/web in Vercel dashboard.

### Wake protocol addition
When Atlas wakes in any of these directories, read this file FIRST after wake.md chain. It tells Atlas what the repo is, what was done, and what CEO expects.
