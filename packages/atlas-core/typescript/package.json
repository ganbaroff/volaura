{
  "name": "@volaura/atlas-core",
  "version": "0.1.0",
  "description": "Canonical Atlas identity + voice validator + ecosystem memory interface for the VOLAURA 5-product ecosystem.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "../identity.json"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --loader ts-node/esm --test tests/*.test.ts"
  },
  "peerDependencies": {
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.39",
    "typescript": "^5.9.3",
    "zod": "^3.25.76"
  }
}
