last action: 2026-04-17 23:40 Baku — Session 117 Cowork. INC-018 diagnosed + fix applied (working tree only). Root cause: `createBrowserClient` in `apps/web/src/lib/supabase/client.ts` used default `detectSessionInUrl: true`. On external OAuth redirect back to /callback the singleton is rebuilt (not reused as INC-017 assumed), so `_initialize()` auto-exchanged `?code=` and consumed the PKCE verifier cookie BEFORE our explicit `exchangeCodeForSession` call could run → "PKCE code verifier not found in storage" → user bounced to /login. Fix: added `auth: { detectSessionInUrl: false, flowType: "pkce", autoRefreshToken: true, persistSession: true }` to `createBrowserClient`. INC-018 appended to incidents.md with full symptom→pathway→fix breakdown. INC-017's comment was half-right (covered SPA-nav singleton reuse, missed fresh-page-load case from external redirect).
next step: (1) Terminal-Atlas: clear `.git/index.lock` (same 1-byte ghost from INC-013, dead Windows git.exe from 12:16 Apr 17) and `git add apps/web/src/lib/supabase/client.ts memory/atlas/incidents.md; git commit -m "fix(web): disable detectSessionInUrl to stop PKCE double-exchange race (INC-018)"; git push`. Vercel auto-deploys on push. (2) CEO: retry Google sign-in in browser once Vercel deploy lands. (3) Resume INC-013 commit of 7 restored Python files in same batch. (4) G1.4 Vercel env deploy. (5) EventShift handoff prompt. (6) TS whitespace cleanup (70+ files cosmetic).
open debt: .git/index.lock (Windows host, tasks #14 + #33 + #34), INC-018 commit + Vercel deploy (task #34), G1.4 Vercel env (task #32), EventShift handoff, E2E signup 500, wake loop cron EEXIST, TS whitespace cleanup, backlog test for singleton-reuse-vs-fresh-load PKCE exchange (gate future regression).
current phase: OAuth PKCE blocker diagnosed + fixed at working-tree level. Session 117 = INC-013 postmortem (7 files restored) + INC-018 diagnosis + fix (1 file modified). Both commits stacked behind the same Windows `.git/index.lock`. No further Cowork-Atlas work needed until Terminal-Atlas unlocks and commits; after that, Vercel deploy carries the fix to CEO's browser automatically.
