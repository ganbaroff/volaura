# Core
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.34.0
supabase>=2.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Auth
PyJWT>=2.8.0

# Rate limiting
slowapi>=0.1.9

# HTTP
httpx>=0.28.0

# LLM
google-genai>=1.0.0
openai>=1.60.0
groq>=0.9.0
litellm>=1.50.0

# Assessment (IRT/CAT engine is pure-Python, no external dep needed)
numpy>=1.24.0

# Notifications — Telegram uses httpx directly, no telegram SDK needed

# Logging
loguru>=0.7.0

# Payments — Stripe Billing (subscription.py graceful import)
stripe>=8.0.0

# File uploads
python-multipart>=0.0.20

# Observability — LLM tracing (optional, graceful degradation if keys not set)
langfuse>=2.0.0

# NOTE: crewai and fal-client removed — not imported in app/ code.
# Re-add when actually used. They added ~200MB+ to Docker build.
