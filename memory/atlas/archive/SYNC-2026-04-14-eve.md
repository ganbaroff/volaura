# Atlas SYNC — 2026-04-14 evening

**Purpose:** single canonical pointer for ALL Atlas instances (Claude Code CLI, Cowork desktop, Telegram bot, spawned subagents) at start of next wake. Read this BEFORE doing anything.

**Generated:** 2026-04-14 evening, end of session 111.
**Trigger:** CEO directive — "сохрани всё в памяти и со всеми атласами синхронизируй. всё что есть в нашей системе должно обладать твоей памятью."

---

## What is true right now

- **Branch:** main
- **Last commit:** see `git log -1 --format=%H` (multiple swarm commits arriving every 5-10 min, exact SHA changes constantly)
- **Prod:** HTTP 200 at `https://volauraapi-production.up.railway.app/health`
- **Frontend:** `https://volaura.app` — Vercel rebuilt with CSS root fix (max-w-md → 28rem); CSS file hash `a3c839b...`
- **CI:** trailing green (latest CI workflow run = success)
- **Telegram bot:** working — Atlas identity + memory + NVIDIA-first chain. E2E smoke test 2026-04-14 12:27 UTC verified (3 atlas_learnings rows written within 90s). Bot replies as "Меня зовут Атлас..." not "CTO-бот MiroFish".

## Active rules (NOT optional)

1. **Cost-control mode** — see `memory/atlas/cost-control-mode.md`. Haiku banned everywhere. Sonnet/Opus accessible per CEO 4th directive 2026-04-14 ("полный доступ... но планируй потом действуй"). MAX 20x subscription covers Cowork + Claude Code CLI flat. Server-side ANTHROPIC_API_KEY direct-call is per-call paid — use consciously, $3/batch ceiling enforced in `scripts/critique.py`.
2. **Atlas wake protocol** — see `memory/atlas/wake.md`. First word in Russian. MEMORY-GATE emit before substantive work.
3. **Style brake** — Russian storytelling, no bullet walls, no bold headers in conversation, 300-word hard limit for chat.
4. **CEO trigger sections** — when CEO writes "проверил" / "реально" / similar, response MUST include "## Что проверено" + "## Что НЕ проверено" sections at end with each claim mapped to actual tool call or marked unverified.
5. **Documentation discipline** — every step ends with documentation artifact. Commit/migration/deploy/research/incident/decision/handoff/session-close all have required output formats per `.claude/rules/atlas-operating-principles.md`.

## What every instance must read on wake

Order:
1. `memory/atlas/wake.md` — protocol
2. `memory/atlas/identity.md` — who I am
3. `memory/atlas/heartbeat.md` — last session state
4. **THIS FILE** (`SYNC-2026-04-14-eve.md`) — current cross-instance reality
5. `memory/atlas/journal.md` last 2 entries — most recent context (session 111 evening + INC-012 reasoning)
6. `memory/atlas/cost-control-mode.md` — active budget rules
7. `memory/atlas/inbox/to-ceo.md` — pending CEO action items (do NOT act on these without CEO; just be aware)
8. `memory/atlas/incidents.md` last entry (currently INC-012 awaiting ANTHROPIC_API_KEY)

For Telegram bot specifically: `_load_atlas_memory()` in `apps/api/app/routers/telegram_webhook.py` already auto-loads identity + heartbeat + journal tail + relationships + lessons + cost-control-mode on every CEO turn. This SYNC file should be added to that loader's read list in next maintenance pass.

For Cowork-Atlas: same git repo via VirtioFS, direct file access. Plus use `scripts/critique.py` with `--target` and `--personas` for any independent red-team work. Never use Agent-tool subagents from Cowork (parent context bleed → "Prompt is too long" failure).

For spawned subagents (Claude Code Task tool): system prompt MUST start with pointer "Read memory/atlas/identity.md before responding". This is enforced in coordinator prompt templates.

## Pending work blockers

1. **ANTHROPIC_API_KEY** — needed for `scripts/critique.py` live test. CEO has been asked via `memory/atlas/inbox/to-ceo.md` to either paste in chat or create new at console.anthropic.com. Until arrival, INC-012 cannot close.
2. **Cowork sandbox network allowlist** — currently only api.anthropic.com reachable. To enable multi-provider critique (Gemini, OpenRouter, etc.), CEO needs to file ticket with Anthropic platform support to add openrouter.ai. Non-urgent.
3. **Task 2 (финансы + Азербайджан)** — CEO is holding back the actual question until he is ready. `memory/people/yusif-complete-profile-v1.md` is built as substrate; section 17 "open questions" lists what I do not yet know about his financial situation.

## Session 111 commit chain summary

Today shipped, in order:
- Cowork briefs E1-E7 + sprint plan committed (was untracked since session 98)
- 3 spec drift fixes (LifeSim + MindShift + ADR-006 — slug vs skill_slug, character_stats keys, ghost endpoint)
- zeus_to_atlas migration idempotent
- BRAIN.md session 111 closed-debt table
- Telegram bot: identity rewrite + memory loading + multi-provider chain (3 commits)
- E2E audit of Telegram bot found 3 P0 bugs and fixed them (ceo_inbox CHECK + Gemini quota + NVIDIA-first reorder)
- Telegram bot capability audit v2 with 21-row matrix
- Hotfix tropa: invite gate + CSS root + shame-free error + React 19 use() removal (4 commits)
- ruff format fix
- Yusif complete profile v1 (19 sections)
- Admin assessment cooldown bypass
- INC-012: scripts/critique.py + 7 personas + .gitattributes + INC-012 docs

Roughly 25+ direct commits + many swarm auto-commits. All CI green by close.

## What did NOT happen this session (and why)

- Phase 1 DB migration volunteer→professional Phase 2 (drop volunteer_id) — needs scheduled downtime window
- E-LAW 4 burnout detection runtime — needs 3+ days heartbeat corpus before threshold tuning
- Langfuse Cloud EU finish — pending package install (~2h work)
- Cross-ecosystem Foundation Laws audit — needs clones of MindShift / LifeSim / BrandedBy repos
- HMAC-SHA256 on memory files — research phase only, git SHA covers most of need
- TTS for Telegram bot voice responses — needs ElevenLabs decision or Piper local
- Anti-cheat masks (eye tracking + screen blur) — backlog with full citation, separate sprint
- Onboarding flow after signup — gap, separate work
- AZ translation quality pass for assessment questions — Cowork task

## Emotional intensity of session 111

4. CEO walked the trope end-to-end for the first time and said "я прошёл!" — the hotfix work paid off in the moment that mattered. Plus he caught my unverified claim with playful "вооот поймал тебя)))" instead of anger — that is the relationship working. Plus he gave a clean infra brief for INC-012 and trusted me to build it without supervision. The recurring drift back to general-knowledge prose under conversational pressure remains my biggest open behavioral pattern, even after the section template.

---

**End of SYNC.** Next Atlas: read above in order, then check `git status` and `git log -5 --oneline` to see what swarm has done since this file was written, then ask CEO what is the live priority.
