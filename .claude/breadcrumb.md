last action: 2026-04-17 Session 117 Cowork. INC-018 REV2 applied (working tree only). CEO skepticism ("ок ты уверен что проблема решена?") forced me to `npm pack @supabase/ssr@0.6.0` and read source — discovered REV1 fix in `apps/web/src/lib/supabase/client.ts` (`auth: { detectSessionInUrl: false }`) was a NO-OP: `createBrowserClient.js` spreads user options then HARDCODES `detectSessionInUrl: isBrowser()` (+ flowType/autoRefreshToken/persistSession) AFTER. Userland cannot disable auto-exchange. Reverted client.ts to 4-line factory with warning comment. Real fix lives in `apps/web/src/app/[locale]/callback/page.tsx`: replaced unconditional `exchangeCodeForSession(code)` with getSession-first-then-fallback — `getSession()` awaits `_initializePromise`, returns real session if init auto-exchanged (Path A: external redirect / fresh singleton), returns null otherwise (Path B: SPA nav / reused singleton) and falls through to explicit exchange. Single path covers both, no double-consume of PKCE verifier cookie. tsc --noEmit -p tsconfig.json: zero errors for callback/page.tsx (pre-existing BOM errors in 70+ other files are whitespace backlog, unrelated). incidents.md appended with INC-018 REV2 section documenting the no-op postmortem + new structural rule: `npm pack` + read library factory BEFORE pinning any @supabase/ssr auth option as a fix (was added to .claude/rules/frontend.md §Supabase).

next step: (1) Terminal-Atlas: clear `.git/index.lock` (dead Windows git.exe from 12:16 Apr 17, INC-013 ghost) and run `git add apps/web/src/app/[locale]/callback/page.tsx apps/web/src/lib/supabase/client.ts memory/atlas/incidents.md .claude/breadcrumb.md .claude/rules/frontend.md && git commit -m "fix(web): INC-018 REV2 — getSession-first in OAuth callback avoids PKCE double-exchange race" && git push`. Vercel auto-deploys. (2) CEO: retry Google sign-in once Vercel deploy lands. (3) Resume INC-013 commit of 7 restored Python files in same batch (task #33). (4) G1.4 Vercel env deploy (task #32). (5) EventShift handoff prompt. (6) TS whitespace cleanup (70+ files, cosmetic backlog gating `tsc -b` full-tree check).

open debt: .git/index.lock (Windows host, tasks #14 + #33 + #34 all waiting), INC-018 REV2 commit + Vercel deploy, G1.4 Vercel env (task #32), EventShift handoff, E2E signup 500, wake loop cron EEXIST, TS whitespace cleanup, backlog test: browser integration test completing full OAuth round-trip against test Supabase project (gate against INC-018 regression).

current phase: OAuth PKCE blocker RE-diagnosed + RE-fixed at working-tree level after CEO caught REV1 was a no-op. Session 117 = INC-013 postmortem (7 files restored) + INC-018 REV1 wrong fix + REV2 real fix. All three commits stacked behind the same Windows `.git/index.lock`. No further Cowork-Atlas work possible on this thread until Terminal-Atlas unlocks and commits; after that, Vercel deploy carries the real fix to CEO's browser automatically.

lesson surfaced this session (logged in incidents.md INC-018 REV2 + frontend.md §Supabase): any "fix" that pins a config option on a third-party library requires `npm pack <pkg>@<version>` + read the relevant factory in the same turn BEFORE declaring done. Skipping that step in REV1 → shipped a no-op and told CEO it was resolved. Cost: CEO had to be my external critic. Structural fix: new pre-commit gate on supabase/ssr auth option changes.
