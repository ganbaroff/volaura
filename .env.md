# Environment Variables Reference

All keys stored in `apps/api/.env` (git-ignored). For production: Railway Secrets.
Updated 2026-04-18 Session 119 (VERTEX_API_KEY rotated, $300 GCP credits active).

## Core Infrastructure (REQUIRED)

| Variable | Provider | Default | Notes |
|----------|----------|---------|-------|
| `SUPABASE_URL` | Supabase | `http://127.0.0.1:54321` | Production: `https://dwdgzfusjsobnixgyzjk.supabase.co` |
| `SUPABASE_SERVICE_KEY` | Supabase | — | Service role key from Supabase dashboard. NEVER commit. |
| `SUPABASE_ANON_KEY` | Supabase | — | Anon/public key for frontend auth |
| `SUPABASE_ANON_JWT` | Supabase | — | Preferred over ANON_KEY. Same key, different field name for JWT validation path. |
| `SUPABASE_JWT_SECRET` | Supabase | — | From Supabase dashboard → Settings → API → JWT Secret. Used for cross-product JWT minting (MindShift bridge). |
| `APP_URL` | — | `http://localhost:3000` | Production: `https://volaura.app`. Used for CORS whitelist + email links. |
| `APP_ENV` | — | `development` | `production` on Railway. Controls CORS strictness + startup assertions. |
| `API_PORT` | — | `8000` | Railway sets `$PORT` automatically. |
| `DEFAULT_LOCALE` | — | `az` | Primary locale for server-generated URLs. |

## LLM Providers (at least GEMINI required)

| Variable | Provider | Cost | Notes |
|----------|----------|------|-------|
| `GEMINI_API_KEY` | Google AI | Free 15RPM | Primary evaluator (BARS). **If set alone without GROQ, falls to paid OpenAI on rate limit (~$240/day).** |
| `GROQ_API_KEY` | Groq | Free 14.4K/day | Secondary fallback. Prevents expensive OpenAI fallthrough. |
| `NVIDIA_API_KEY` | NVIDIA NIM | Free | Telegram bot primary provider. Nemotron Ultra for swarm judge role. |
| `DEEPSEEK_API_KEY` | DeepSeek | $0.14/MTok | Swarm experimental. |
| `OPENAI_API_KEY` | OpenAI | PAID $5 cap! | Anti-gaming tertiary fallback ONLY. Set billing cap in dashboard. |
| `OPENROUTER_API_KEY` | OpenRouter | Varies | Multi-provider gateway. Currently Cowork sandbox can't reach it (INC-012). |
| `VERTEX_API_KEY` | Google Vertex | $300 GCP credits | Express API key format `AQ.Ab8...`. Gemini paid fallback. Rotated 2026-04-18 by CEO — credits must be actively used (proactive-credits rule). |
| `CEREBRAS_API_KEY` | Cerebras | Free | 2000+ tok/s. Used by swarm engine. Constitution hierarchy #1. |
| `GCP_PROJECT_ID` | Google Cloud | — | Translation API project. Optional. |

## Telegram Bot

| Variable | Provider | Notes |
|----------|----------|-------|
| `TELEGRAM_BOT_TOKEN` | Telegram | `@volaurabot` token. `7NNNNNN:AA...` format. |
| `TELEGRAM_CEO_CHAT_ID` | Telegram | CEO's personal chat ID for HIGH/CRITICAL alerts. |
| `TELEGRAM_WEBHOOK_SECRET` | Self-gen | HMAC validation on incoming webhooks. If empty → endpoint 403 (fail-closed). |

## Payments (kill-switched)

| Variable | Provider | Default | Notes |
|----------|----------|---------|-------|
| `PAYMENT_ENABLED` | — | `false` | Master kill switch. Set `true` only after Stripe keys configured. |
| `STRIPE_SECRET_KEY` | Stripe | — | `sk_test_...` (test) or `sk_live_...` (production). Full account access — dev/fallback only. |
| `STRIPE_PUBLISHABLE_KEY` | Stripe | — | `pk_test_...` / `pk_live_...`. Safe for client-side (Checkout.js, Elements). |
| `STRIPE_RESTRICTED_KEY_TEST` | Stripe | — | `rk_test_...` scope-limited key. Preferred for backend prod paths (Checkout write + Customers read + Webhooks read). |
| `STRIPE_PRICE_ID` | Stripe | ✅ Session 119 | `price_1TMmXICVasIpbKGIwuPdr2am` — VOLAURA Pro $9.99/mo recurring on product `prod_ULTUzKXfV0qdF2`. Created via Stripe API (urllib + Basic auth, not Dashboard). Saved to .env + GH Secrets 2026-04-18. |
| `STRIPE_WEBHOOK_SECRET` | Stripe | ✅ Session 119 | `whsec_yR99p4...` for endpoint `we_1TNZJyCVasIpbKGIQRSJxqm1` → `https://volauraapi-production.up.railway.app/api/subscription/webhook`. 5 events: checkout.session.completed, invoice.payment_failed, customer.subscription.{created,updated,deleted}. Rotated via API 2026-04-18 (old `we_1TMmXICVasIpbKGIkCp2Ycrj` deleted — Stripe returns webhook secret only on CREATE). |

## Email (kill-switched)

| Variable | Provider | Default | Notes |
|----------|----------|---------|-------|
| `EMAIL_ENABLED` | — | `false` | Master kill switch for transactional email. |
| `RESEND_API_KEY` | Resend | — | Transactional email service. Set on Railway. |

## Observability

| Variable | Provider | Notes |
|----------|----------|-------|
| `SENTRY_DSN` | Sentry | `https://xxx@oNNN.ingest.de.sentry.io/NNN`. Error tracking. If empty → startup warning, no crash. |
| `LANGFUSE_PUBLIC_KEY` | Langfuse | `pk-lf-...` from cloud.langfuse.com → Settings → API Keys. |
| `LANGFUSE_SECRET_KEY` | Langfuse | `sk-lf-...` same dashboard. |
| `LANGFUSE_HOST` | Langfuse | Default `https://cloud.langfuse.com` (EU). US: `us.cloud.langfuse.com`. |

## Security & Webhooks

| Variable | Provider | Notes |
|----------|----------|-------|
| `SENTRY_WEBHOOK_SECRET` | Self-gen | HMAC-SHA256 for `/api/webhooks/sentry`. Generate: `python -c "import secrets; print(secrets.token_urlsafe(48))"`. |
| `GITHUB_PAT_ACTIONS` | GitHub | PAT with `repo` + `issues:write`. Used by Sentry webhook → auto-create `needs-RCA` issues. Also by `fix-of-fix-detector.yml`. |
| `GITHUB_REPO` | — | Default `ganbaroff/volaura`. Override for forks only. |
| `CRON_SECRET` | Self-gen | Internal auth for GitHub Actions cron endpoints. If empty → tribe matching + other scheduled jobs fail 403. |
| `E2E_TEST_SECRET` | Self-gen | Bypass token for E2E smoke tests. Never exposed to frontend. |
| `GATEWAY_SECRET` | Self-gen | `X-Gateway-Secret` header for Atlas gateway (`/api/atlas/proposal`). If empty → 403 fail-closed. |
| `EXTERNAL_BRIDGE_SECRET` | Self-gen | VOLAURA ↔ MindShift edge function bridge auth. |

## Cross-Product

| Variable | Provider | Notes |
|----------|----------|-------|
| `MINDSHIFT_URL` | — | e.g. `https://mindshift.app`. Added to CORS whitelist in production. |
| `SUPABASE_URL_MINDSHIFT` | Supabase | MindShift Supabase project URL (separate instance). |
| `SUPABASE_ANON_KEY_MINDSHIFT` | Supabase | MindShift anon key. |

## BrandedBy (Phase 1-2)

| Variable | Provider | Notes |
|----------|----------|-------|
| `DID_API_KEY` | D-ID | Lite plan $5.90/mo. Video avatar generation. |
| `FAL_API_KEY` | fal.ai | MuseTalk + Kling LipSync. DSP winner for video pipeline. |

## Access Control

| Variable | Default | Notes |
|----------|---------|-------|
| `OPEN_SIGNUP` | `false` | If `true`, anyone can register. If `false`, invite code required. |
| `BETA_INVITE_CODE` | — | Gate code for beta signup. Set via Railway env. |

## Specialized Services

| Variable | Provider | Notes |
|----------|----------|-------|
| `MEM0_API_KEY` | Mem0 | Semantic memory. Used by `atlas_heartbeat.py` + `atlas_recall.py`. |
| `TAVILY_API_KEY` | Tavily | Web search with citations. Fallback when WebSearch tool insufficient. |
| `DODO_PAYMENTS_API_KEY` | Dodo | AZ-local payment rail alternative. Currently 0 code imports — placeholder only. |
| `RAILWAY_API_TOKEN` | Railway | ✅ Session 120 | Workspace-scoped token for `Yusufus's Projects` (id `df200199-7e84-4d5a-92be-b621d8da8f43`). Added 2026-04-18 for VERTEX_API_KEY propagation to `@volaura/api` service (project `zesty-art` / id `9e1940ed-038d-4d42-a79d-b8430034e697`, env `production` / id `6474308a-4b75-4035-959d-16e703362ad5`, service id `3902da60-d560-475d-8978-4a6871d4c2de`, public URL `volauraapi-production.up.railway.app`). Auth: `Authorization: Bearer` on `https://backboard.railway.com/graphql/v2`. Token is `apiToken`-type (returns `ApiTokenContext { workspaces }`, not `User` — `me` query fails; use `apiToken { workspaces { id name projects { id name services { edges { node { id name } } } } } }` instead). IMPORTANT: Cloudflare blocks Python `urllib` default User-Agent with error 1010 — use `curl` or set `User-Agent: Mozilla/5.0` header. Env var CRUD via `variableUpsert(input: VariableUpsertInput!)` / `variableDelete(input: VariableDeleteInput!)`. |

## Security Checklist
- [ ] OpenAI billing cap set to $5/mo in dashboard
- [ ] GCP quota set for Gemini API
- [ ] Railway Secrets used (not .env file upload)
- [ ] Separate dev/prod keys created
- [ ] Supabase service key rotated after initial dev
- [ ] CRON_SECRET set on Railway (tribe matching fails 403 without it)
- [ ] TELEGRAM_WEBHOOK_SECRET set on Railway (fail-closed)
- [ ] SENTRY_WEBHOOK_SECRET set on Railway + Sentry dashboard
- [ ] All self-gen secrets generated via `python -c "import secrets; print(secrets.token_urlsafe(48))"`
